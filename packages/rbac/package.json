{
  "name": "@yourorg/rbac",
  "version": "0.1.0",
  "private": true,
  "description": "Single source of truth for Norr's RBAC permission matrix (lib/rbac/permissions.ts re-exports this). Lives inside this monorepo (npm workspace package, not a published registry package) so every app in the workspace (root app, apps/pwa, ...) imports the exact same permission logic instead of drifting copies.",
  "type": "module",
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.cjs"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "tsup": "^8.3.5",
    "typescript": "^5"
  }
}
