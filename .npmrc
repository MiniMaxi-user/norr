# @yourorg/ui is a real npm workspace package that lives in this repo
# (packages/ui) — there is no separate design-system repo/registry to point
# at. npm resolves it via workspace linking (see root package.json's
# "workspaces" field and its "@yourorg/ui": "*" dependency range), so no
# scoped registry configuration is needed here.
