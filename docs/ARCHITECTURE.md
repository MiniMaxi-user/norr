# Architecture

## Interpretation of roles — confirm before Phase 1
Two-level tenancy:
- **Platform Admin**: operates the SaaS, manages tenant organizations, billing, cross-tenant analytics.
- **Tenant roles** (inside one organization = one "Client"): `owner` (full access), `planner`, `engineer`, `finance`, `administratie`.

The **Clients module is the tenant's own customer records** (their CRM) — not the tenant itself. If this is wrong, flag it before Phase 1 starts: it changes the schema.

## Stack
- Next.js 15 (App Router, Server Components by default), TypeScript strict
- Supabase: Postgres, Auth (email/password + magic link, SSO later), Storage, Row Level Security
- Stripe: Billing (subscriptions, per-module price items), webhooks → entitlements
- Vercel: hosting, preview deployment per PR
- Design system: `@yourorg/ui`, an npm workspace package living in this same repo at `packages/ui` (not a separate repo/registry) — see "Design system consumption" below

## Multi-tenancy & data isolation
- `organizations` = tenants
- `platform_admins` — separate from tenant roles (e.g. `users.is_platform_admin boolean`), never assignable through tenant-facing invite flows
- `memberships (user_id, organization_id, role)` — role enum: `owner | planner | engineer | finance | administratie`
- Every tenant-scoped table carries `organization_id`. RLS policy pattern — implemented (see `supabase/migrations/20260822150910_organizations_memberships_baseline_rls.sql`) via a `SECURITY DEFINER` helper function rather than an inline scalar subquery:

```sql
-- Helper (defined once, in the baseline migration). SECURITY DEFINER lets it
-- read `memberships` bypassing RLS, which is what avoids "infinite
-- recursion detected in policy" when used inside memberships' own policies,
-- and also avoids "more than one row returned by a subquery" if a user is
-- ever a member of more than one organization (a plain scalar-equality
-- subquery breaks in that case).
create or replace function public.is_member_of_org(org_id uuid)
returns boolean
language sql stable security definer set search_path = public
as $$
  select exists (
    select 1 from public.memberships m
    where m.organization_id = org_id and m.user_id = auth.uid()
  );
$$;

-- Every tenant-scoped table's policy:
create policy "org_isolation" on assets
  using (is_member_of_org(organization_id));
```

  Use `is_member_of_org(organization_id)` (and, where a write should be owner-only, `is_org_owner(organization_id)`) in every new tenant-scoped table's RLS policies — don't re-derive membership with an inline subquery.
  - **Tenant activate/deactivate** — `organizations.is_active boolean not null default true`, added in `supabase/migrations/20260826120000_organizations_is_active.sql` (issue #47). `is_member_of_org`/`is_org_owner` were extended in that same migration to additionally require `o.is_active` on the target organization (join to `organizations`, same signatures, no caller changes needed anywhere) — this single choke point makes an entire deactivated tenant's data invisible/unwritable under RLS to that org's own members, across every tenant-scoped table that already calls one of these two helpers, with zero per-table changes. `organizations_select_creator_or_member`'s `created_by = auth.uid()` branch deliberately does **not** require `is_active` (unaffected, since it doesn't call either helper) — the Platform Admin who created a tenant org via `activateAsTenant` must still see/manage that row while deactivated, in order to reactivate it. `memberships_select_self_or_same_org`'s `user_id = auth.uid()` self-visibility branch was also updated in that migration to require the row's own org to be active (checked directly against `organizations`, not `memberships`, so it doesn't reintroduce the same-command MVCC recursion issue `20260822173916_fix_memberships_self_visibility.sql` fixed) — otherwise a deactivated org's member could still read their own membership row via that bypass. Reactivating (`is_active = true`) restores exactly the prior access; deactivate/reactivate never mutates membership rows themselves. The deactivate/reactivate Server Action and login-time gate are `auth-rbac-engineer` follow-ups on top of this column; the UI button/badge is a `frontend-ui-engineer` follow-up.
  - **`current_member_role(org_id uuid) returns membership_role`** — added in `supabase/migrations/20260823120000_work_orders_core.sql` (issue #13), `SECURITY DEFINER`/`stable` like the two helpers above. Returns the caller's role in `org_id`, or `NULL` if they have no membership there at all. `is_member_of_org`/`is_org_owner` only answer yes/no questions; once a table's RBAC matrix row needs a real 3+-way split (e.g. `work_orders`: owner/planner full, engineer assigned-rows-only, finance/administratie read-only-all-rows), policies branch on `current_member_role(organization_id) in (...)` / `= 'engineer'` instead of stacking more boolean helpers. Reuse this for Assets' own still-deferred Planner/Engineer split and for a future Contracts table's Finance-CRUD boundary — don't reinvent a role check inline.
- No client-side query ever bypasses RLS. Server actions run under the user's session. The service-role key is used only in trusted server-only contexts (billing webhook sync, audited platform-admin cross-tenant reads). Platform Admin cross-tenant reads are **not** implemented as an RLS bypass policy for `is_platform_admin` users — they go through `lib/supabase/admin.ts` (service-role) from trusted server code only, audited in application code.

### Auth flows — implemented (issue #3)
- `lib/auth/session.ts`: `getCurrentSession()` / `requireSession()` resolve the signed-in user, their `is_platform_admin` flag, and their (first) organization + tenant role in one place, under the caller's own session. `app/(app)/layout.tsx` calls `requireSession()` to gate the entire authenticated route group (redirects to `/login` when signed out); nothing else under `app/(app)` needs its own "is anyone signed in" check.
- `lib/auth/actions.ts`: Server Actions for signup, login, logout, invite creation, and invite redemption — all via `lib/supabase/server.ts` (never `admin.ts`). This Supabase project requires email confirmation (`mailer_autoconfirm: false`), so a brand-new signup has no session in the same request; organization/owner bootstrap is deferred to that user's first successful login (see `ensureOwnOrganizationBootstrapped` — carries the org name through via Supabase user metadata across the confirmation round trip).
- `app/(auth)/{login,signup,invite/[token]}`: route group without the `AppShell` chrome (sibling to `app/(app)`), using `@yourorg/ui` form primitives (`Input`/`Label` — see `packages/ui`, "Design system consumption" below).
- Invite mechanism: see the `invites` table entry under "Core schema (v1)" above.

## Core schema (v1)
- `organizations`, `memberships`, `users` (Supabase `auth.users` + profile table) — **implemented** in `supabase/migrations/20260822150910_organizations_memberships_baseline_rls.sql`:
  - `users (id uuid pk references auth.users, email, full_name, is_platform_admin boolean default false, created_at, updated_at)` — profile row auto-created by an `on auth.users insert` trigger; `authenticated` only has column-level UPDATE grant on `full_name` (never `is_platform_admin`/`email`/`id`), so no client path can self-elevate to platform admin.
  - `organizations (id uuid pk, name, slug unique, created_by references users, is_active boolean default true, created_at, updated_at)` — visible to its creator (`created_by = auth.uid()`) even before any membership exists (needed for `INSERT ... RETURNING` during signup bootstrap), and to any member thereafter. **`is_active`** added by `supabase/migrations/20260826120000_organizations_is_active.sql` (issue #47) — see the "Tenant activate/deactivate" bullet under "Multi-tenancy & data isolation" above for the full RLS mechanism.
  - `memberships (id uuid pk, user_id references users, organization_id references organizations, role membership_role not null, created_at, updated_at, unique(user_id, organization_id))` — `role` enum: `owner | planner | engineer | finance | administratie`.
  - Bootstrapping (first org + first owner membership on signup): a user may self-insert an `owner` membership into an organization only if (a) they created that organization and (b) it has zero existing members. After that, only existing `owner` members can insert further memberships — this is the hook the invite flow (issue #3/#4) should use.
- `clients`, `sites`, `assets` — **implemented** (issue #7) in `supabase/migrations/20260822190000_clients_sites_assets.sql`, tested in `supabase/tests/database/clients_sites_assets_rls.test.sql`:
  - `clients (id, organization_id, name, kvk_number, vat_number, iban, notes, created_by, created_at, updated_at)` — the tenant's own customer records (their CRM), not the tenant itself. `organization_id` is supplied directly by the inserting owner (checked by RLS, like `invites.organization_id`). **Its original flat `address_line1`/`address_line2`/`postal_code`/`city`/`country` columns were dropped** by `supabase/migrations/20260825090000_sites_addresses.sql` (issue #41 redo) — `sites` (below) is now the sole, multi-address model for a client's address(es); see that bullet. **`email` dropped, `kvk_number`/`vat_number`/`iban` added** by `supabase/migrations/20260825150000_clients_business_fields.sql` (issue #43, "Breid client uit") — a client's contact email now comes from a `contacts` row (`contacts.email`) or isn't tracked at the client level; `kvk_number` (Dutch Chamber of Commerce number), `vat_number` (VAT/BTW number), and `iban` (bank account IBAN) are plain `text` with no DB-layer format validation. **`represents_organization_id`** (nullable, unique-when-set FK into `organizations`, `on delete set null`) was added by `supabase/migrations/20260825160000_clients_represents_organization.sql` (issue #45, "Platform Admin managing tenants by reusing the Clients module") — when set, this Client row IS a real platform tenant: the Platform Admin operates as an `owner` member of a dedicated "Platform" organization (bootstrapped by hand, not by this migration) and manages tenants as `clients` rows within that org, same as any tenant manages its own CRM clients; `activateAsTenant` (`app/(app)/clients/actions.ts`) creates the linked `organizations` row and sets this column. No new/changed RLS policy was needed — `clients_update_owner`'s `is_org_owner(organization_id)` check is keyed on the client row's OWN (Platform) `organization_id`, which the Platform Admin already owns, exactly like any other client update. Granted on UPDATE only (not INSERT) — a client is never created already representing a tenant. **`phone` dropped** by `supabase/migrations/20260826130000_sites_phone.sql` — moved to `sites.phone` (below); a client can have multiple sites, each with its own contact number, so phone belongs on the physical location, not the client record.
  - `sites (id, organization_id, client_id, phone, address_line1, address_line2, postal_code, city, country, latitude, longitude, geocoded_at, notes, is_visit_address, is_invoice_address, is_delivery_address, is_primary, created_by, created_at, updated_at)` — a client's physical location, and (as of `20260825090000_sites_addresses.sql`, issue #41 redo, "Sites as client addresses") the client's address book: a first pass of that story built a brand-new `client_addresses` table before being corrected — `sites` already served this role (it's also the location `assets` hang off of via `assets.site_id`; that dual role is intentional, not split apart). `latitude`/`longitude` are plain `double precision` (no PostGIS dependency) — sufficient for map-view pins; revisit only if geospatial queries (radius search etc.) are actually needed. **Repurposed by that same migration** from manually-entered coordinates to a server-computed geocoding cache: `geocoded_at timestamptz` (nullable) records when they were last computed from the address fields by `lib/geocoding/nominatim.ts` (free, no-API-key OSM Nominatim search), written by the `createSite`/`updateSite` server action, not a DB trigger — "the pin follows the address," not manual lat/long entry. **`name` column dropped** by `supabase/migrations/20260825120000_sites_drop_name.sql` (issue #42) — a site is identified purely by its formatted address everywhere in the UI now (e.g. "Vleutenseweg 22, Utrecht"), not a free-text label; the column-level INSERT/UPDATE grants were already re-issued without `name` in that same migration (Postgres drops a column's own grant entries automatically on `DROP COLUMN`, so no separate `revoke` was needed). **`phone` added** by `supabase/migrations/20260826130000_sites_phone.sql` — moved from `clients.phone` (a client's phone now lives on the site/location it belongs to, not the client record); plain nullable `text`, no DB-layer format validation, granted on both INSERT and UPDATE (unlike `clients.represents_organization_id`, a site's phone is a normal field settable at creation time).
    - **Purpose flags**: `is_visit_address`/`is_invoice_address`/`is_delivery_address` booleans (visit address/invoice address/delivery address) — a row-level CHECK (`sites_at_least_one_purpose`) requires at least one true per site. Every site that existed before this migration was backfilled `is_visit_address = true` (a site was already, by construction, a place a technician visits). **Cross-row invariant, deliberately NOT DB-enforced**: across ALL of a client's sites, each of the three purposes must be covered by at least one site (a single site may cover more than one) — this is a per-client aggregate check a plain row CHECK can't express; it's `api-backend-engineer`'s responsibility in `createSite`/`updateSite`/`deleteSite` to reject a write that would leave a purpose uncovered for that client.
    - **`is_primary`**: at most one `true` per `client_id` — the client's main address, badged in overviews/detail pages. Enforced by `enforce_single_primary_site` (auto-unsets the previous primary on INSERT/UPDATE OF `is_primary`) plus `sites_one_primary_per_client_idx` (partial unique index), mirroring `contacts.is_primary`/`enforce_single_primary_contact` exactly.
    - **Legacy-address backfill**: every client with a non-null legacy `address_line1` and no existing `sites` row got exactly one new site (`name = 'Main address'`, all three purpose flags true, `is_primary = true`) created from that data before the flat columns were dropped from `clients`. A client that already had sites AND a non-null flat address had that flat data dropped, not migrated (judgment call — no reliable way to know which existing site it corresponded to).
  - `assets (id, organization_id, client_id, site_id, name, type_id, manufacturer, model, serial_number, status_id, installed_at date, warranty_until date, notes, created_by, created_at, updated_at)` — equipment installed at a site. `type_id`/`status_id` are FKs into `reference_list_items` (see "Tenant-configurable reference data" below) — **as of `20260822200000_reference_lists.sql`**, these replaced the original free-text `type` column and the original `asset_status` enum column (`'active'|'decommissioned'`), because a Postgres enum cannot gain a per-tenant value without a schema migration and free text has no governance at all. `status_id` defaults to the organization's default `asset_status` item when omitted on insert (same UX the old `default 'active'` had); `type_id` is required, no default, same as the old `type` column.
  - **`organization_id` denormalization decision**: `organizations -> clients -> sites -> assets` is a 2-3 hop chain. Rather than write RLS policies that join through `clients` (and, for `assets`, through `sites` *and* `clients`), both `sites` and `assets` carry their own `organization_id` column (and `assets` also carries `client_id`, denormalized from its `site_id`), so every tenant-scoped table keeps the same simple, single-column `is_member_of_org(organization_id)` / `is_org_owner(organization_id)` RLS policy shape — no in-policy joins, cheaper to plan. These denormalized columns are populated automatically by a `BEFORE INSERT/UPDATE OF <parent-fk>` trigger (`derive_site_organization_id`, `derive_asset_org_and_client`) and are **excluded from the client-facing column-level INSERT/UPDATE grants** (same lockdown pattern as `invites.token`/`accepted_at`) — callers must supply `client_id` (sites) / `site_id` (assets) only; the trigger derives and overwrites `organization_id`/`client_id` regardless of what's sent. The same triggers also reject any UPDATE that would re-parent a row across organizations (raises `23514`), since a plain RLS `USING`/`WITH CHECK` on `organization_id` alone can't express "and don't let this row move to a different tenant."
  - **Write RLS boundary (v1, deliberately coarse)**: SELECT is `is_member_of_org(organization_id)` (any role) on all three tables. INSERT/UPDATE/DELETE is `is_org_owner(organization_id)` only, on all three tables — this matches the RBAC matrix's "Owner: CRUD" column but does **not** yet implement Planner's "Read/Update" or Engineer's "Read/Update (assigned)" on Assets at the RLS layer; that finer split is an application-layer (`lib/rbac/permissions.ts`) concern until Phase 2's assignment-based scoping lands. A Planner/Engineer session cannot write these tables directly today — non-owner writes (once built) must go through a server-side path that checks `can()` and uses the service-role client.
  - `created_by` on all three tables is trigger-stamped (`set_created_by`, `auth.uid()`) and excluded from client-facing grants, same pattern as `invites`/`users.is_platform_admin`.
  - **Live-tested lesson, fixed in `supabase/migrations/20260822193000_fix_clients_sites_assets_column_grants.sql`**: this project's `public` schema grants ALL table privileges to `authenticated`/`anon` by default on every newly created table. A column-restricted `grant insert (subset of columns)` / `grant update (subset of columns)` is purely additive and does **not** revoke the pre-existing unrestricted privilege on the other columns — you must `revoke all on <table> from authenticated;` first, then grant back exactly the intended columns (this is what `users`' `is_platform_admin` lockdown already did correctly in the baseline migration; the first cut of this migration omitted the revoke and was confirmed live to leak column-level INSERT/UPDATE on `created_by`/`organization_id`/`client_id` before the fix). Apply this `revoke all` step for every future tenant-scoped table that needs any column-level write restriction — don't assume a fresh table starts with zero grants for `authenticated`.
- **Tenant-configurable reference data (picklists)** — **implemented** in `supabase/migrations/20260822200000_reference_lists.sql`, tested in `supabase/tests/database/reference_lists_rls.test.sql`. Generalizes "Asset Type" / "Asset Status" (and every future tenant-configurable dropdown, e.g. Phase 2's Contract Type) into one reusable pattern instead of a hardcoded enum (can't gain a per-tenant value without a migration) or free text (no governance):
  - `reference_lists (id, organization_id, list_key, name, created_by, created_at, updated_at)` — one picklist container per `(organization_id, list_key)`, e.g. `list_key = 'asset_type'`. `list_key` is plain text (not an enum) specifically so a brand new picklist never needs a schema migration to introduce, only a data-seeding change.
  - `reference_list_items (id, reference_list_id, organization_id, value, label, color, sort_order, is_default, created_by, created_at, updated_at)` — the individual selectable values within a list (e.g. `value='hvac', label='HVAC'` inside the org's `asset_type` list). `value` is a stable machine slug (unique per list); `label` is the tenant-editable display text. At most one item per list has `is_default = true` (enforced by `enforce_single_default_reference_item` + a partial unique index) — used to auto-fill e.g. `assets.status_id` when a caller omits it on insert.
  - **Pattern choice, documented in the migration**: a generic reference-list pattern (one schema/RLS/seed-trigger surface shared by every picklist) was chosen over dedicated tables per concept (`asset_types`/`asset_statuses`) specifically because more of these are coming (Contract Type in Phase 2, likely SLA tier/priority later) and this codebase has already shipped the grant-lockdown boilerplate wrong twice on the first pass (see the two `fix_*_column_grants` migrations below) — minimizing how many times that boilerplate gets written is a real risk reduction. The trade-off: every query against a picklist's items is one join deeper than a dedicated table, and there's no per-concept FK typing (nothing at the schema level alone stops pointing `assets.type_id` at an `asset_status` item) — closed instead by the `validate_asset_reference_items` trigger on `assets`.
  - **`organization_id` denormalization**: `reference_list_items.organization_id` is denormalized from `reference_lists.organization_id` (via `reference_list_id`), kept in sync by the `derive_reference_list_item_org` trigger — same reasoning and shape as the `sites`/`assets` denormalization above, so RLS stays a single-column `is_member_of_org(organization_id)`/`is_org_owner(organization_id)` check with no in-policy joins. Unlike `sites.client_id`/`assets.site_id`, `reference_list_id` is NOT re-parentable — it's excluded from the UPDATE column grant entirely (moving "HVAC" from the `asset_type` list to the `asset_status` list is meaningless), so there's no cross-org re-parent guard to test on that column, only on INSERT.
  - **RLS**: SELECT is `is_member_of_org(organization_id)` (any role — every Select dropdown in the app needs this); INSERT/UPDATE/DELETE is `is_org_owner(organization_id)` only — same write boundary as `clients`/`sites`/`assets`.
  - **Seeded defaults on organization creation**: `seed_default_reference_lists(organization_id)` (idempotent, `on conflict do nothing` throughout) is called automatically by an `after insert on organizations` trigger (`organizations_seed_reference_lists`, SECURITY DEFINER — same pattern as `handle_new_auth_user`), so every new organization gets a starting `asset_type` list (HVAC, Electrical, Plumbing, Generator, Other — Other is the default) and `asset_status` list (Active [default], In Repair, Decommissioned) with zero application-layer effort. The one organization that already existed before this migration was backfilled once, directly in the migration, by the same idempotent function. **Any future tenant-configurable picklist (e.g. Phase 2 Contract Type) should extend `seed_default_reference_lists` with another `list_key` block plus a one-time backfill call in that feature's own migration — do not invent a new seeding mechanism.**
  - **What a client needs to query**: to populate a "select asset type" dropdown — `GET /reference_list_items?select=id,value,label,sort_order,is_default&reference_lists!inner(list_key)&reference_lists.list_key=eq.asset_type&order=sort_order` (RLS scopes it to the caller's own org automatically; no explicit `organization_id` filter needed, though the app may still want a stable server-side org context). To let an owner add a new value — first `GET /reference_lists?select=id&list_key=eq.asset_type` to get that org's list id, then `POST /reference_list_items` with `{ reference_list_id, value, label, sort_order? }` (as an `is_org_owner` session; `organization_id` is derived automatically and must not be supplied).
  - **Known gap, RESOLVED for the backend half**: `app/(app)/assets/schema.ts` (`assetCreateSchema`/`assetUpdateSchema`) and `app/(app)/assets/actions.ts` originally still targeted the OLD `type`/`status` columns (free text / `assetStatusSchema` enum) after this migration renamed those to `type_id`/`status_id` (uuid FKs) and dropped the old columns entirely. `api-backend-engineer` has since updated both files: `assetCreateSchema`/`assetUpdateSchema` now take `typeId`/`statusId` (uuid, `statusId` optional), and every `AssetRecord`-returning query in `actions.ts` selects `*, asset_type:reference_list_items!assets_type_id_fkey(value,label,color), asset_status:reference_list_items!assets_status_id_fkey(value,label,color)` (FK constraint names confirmed live — Postgres's default `<table>_<column>_fkey` naming for an unnamed column FK) so the frontend gets resolved type/status label+color in one round trip. `lib/actions/result.ts`'s `mapDbError` was also generalized so `23514` (wrong `list_key`/cross-org reference) and `23503` (dangling reference, in either direction — including the FK-restrict-on-delete case for an in-use `reference_list_items` row) both surface as clean messages. New `lib/reference-lists/{schema,actions}.ts` (owner-only CRUD, any-member read, gated on a new `settings` entry in `lib/rbac/permissions.ts` + `lib/rbac/features.ts`) manages `reference_lists`/`reference_list_items` directly, for reuse by any future picklist. **Still open, for `frontend-ui-engineer`**: the asset form UI (`app/(app)/assets/components/*`, `app/(app)/assets/[id]/page.tsx`) still reads the old `type`/`status` fields off `AssetRecord` and needs updating to `typeId`/`statusId` + the new `asset_type`/`asset_status` embeds, and there's no Settings page yet for managing picklist values.
- **Dependent reference lists (generic capability)** — **implemented** (issue #26) in `supabase/migrations/20260823090000_contacts_dependent_reference_lists.sql`. A reference list's valid items can depend on which item was picked from another list (e.g. Asset Sub-type depends on Asset Type) — this is a standing case per "Domain completeness" below, not a one-off:
  - `reference_lists.parent_list_key text` (nullable) — when set, declares that every item in THIS list must belong under an item from the list with `list_key = parent_list_key`, in the same organization. Plain text, not a FK, because `list_key` alone isn't unique (uniqueness is `(organization_id, list_key)`) — resolving "which list" is organization-scoped and done inside the validation trigger, not declaratively. Checked (`reference_lists_no_self_parent`, `reference_lists_parent_list_key_format`) but not otherwise validated against an actually-existing list at the container level — enforcement happens the moment items are inserted, per below. Insert-only in the client-facing grants (a list's dependency is a fixed structural property, not something meaningfully re-parented later), same immutability stance as `reference_list_items.reference_list_id`.
  - `reference_list_items.parent_item_id uuid references reference_list_items(id)` (nullable) — required, and validated, exactly when the item's own list has a non-null `parent_list_key`. Enforced by `validate_reference_list_item_parent` (mirrors `validate_asset_reference_items`'s structure): rejects a missing `parent_item_id` when required, a parent from the wrong `list_key`, or a parent from a different organization (all `23514`/`23503`).
  - Generic and reusable — not hardcoded to Asset Sub-type/Asset Type. **Second pilot, confirming the mechanism generalizes**: `sla_tier` (`list_key='sla_tier'`, `parent_list_key='contract_type'`), seeded by `supabase/migrations/20260823150000_contracts_core.sql` alongside `contracts.sla_tier_id` (see "Core schema (v1)" below) — exactly new seed rows plus one cross-field check in `validate_contract_reference_items`, no new schema/trigger surface, as predicted.
  - **Pilot**: `asset_subtype` (`list_key='asset_subtype'`, `parent_list_key='asset_type'`) is seeded per organization by `seed_default_reference_lists` with items per existing `asset_type` value (HVAC → Compressor/Thermostat/Ductwork; Electrical → Panel/Wiring/Lighting; Plumbing → Pipe/Valve/Pump; Generator → Engine/Transfer switch; Other → Other), each with `parent_item_id` pointing at its matching `asset_type` item in the same organization. `assets.subtype_id uuid references reference_list_items(id)` (nullable) is validated by extending (not duplicating) `validate_asset_reference_items`: must be an `asset_subtype` item in the asset's own organization (already guaranteed at the list level by `validate_reference_list_item_parent`) AND its `parent_item_id` must equal the asset's own `type_id` — the actual cross-field dependency check, which the generic list-level trigger cannot express since it only knows about the `reference_list_items` graph, not about `assets`.
- `contacts` — **implemented** (issue #26) in `supabase/migrations/20260823090000_contacts_dependent_reference_lists.sql`, tested in `supabase/tests/database/contacts_dependent_reference_lists_rls.test.sql`. A client's contact persons (multiple per client) — the first concrete application of the "Domain completeness"/"Relational detail pages" standard below to an existing module:
  - `contacts (id, organization_id, client_id, name, role_item_id, email, phone, is_primary, notes, created_by, created_at, updated_at)`. Same shape/pattern as `sites`: `organization_id` is denormalized from `clients.organization_id` via `client_id` (`derive_contact_organization_id`, mirrors `derive_site_organization_id`, including the cross-org re-parent guard), so RLS stays the same single-column `is_member_of_org(organization_id)`/`is_org_owner(organization_id)` shape.
  - `role_item_id` is a nullable FK into `reference_list_items`, validated by `validate_contact_role_item` to be from that organization's `contact_role` list (seeded per-org: Primary [default], Billing, Site manager, Technical) — same structural style as `validate_asset_reference_items`.
  - At most one `is_primary = true` per `client_id`, enforced by `enforce_single_primary_contact` (auto-unsets the previous primary, mirrors `enforce_single_default_reference_item`) plus a partial unique index (`contacts_one_primary_per_client_idx`) backstop.
  - **RLS/write boundary**: identical to `sites` — select `is_member_of_org`; insert/update/delete `is_org_owner` only (same v1 coarse boundary, same known Planner/Engineer gap noted above for assets).
  - **Done**: surfaces as a `Tabs` entry on the client detail page (`app/(app)/clients/[id]/client-detail.tsx`, `contacts-panel.tsx`), created in-context (client already set) via `contact-form-dialog.tsx` — per "Relational detail pages" below, not a disconnected top-level route.

- `work_orders` — **implemented** (issue #13, Phase 2) in `supabase/migrations/20260823120000_work_orders_core.sql`, tested in `supabase/tests/database/work_orders_rls.test.sql`. The first-class job/ticket entity (previously only implicit inside Planning) — supersedes the placeholder note this bullet used to carry:
  - `work_orders (id, organization_id, client_id, site_id, asset_id, assigned_to, title, description, notes, status_id, priority_id, scheduled_at, completed_at, contract_id, created_by, created_at, updated_at)`. `organization_id` is denormalized from `clients.organization_id` via `client_id` (`derive_work_order_organization_id`, same shape as `sites`/`contacts`). `site_id`/`asset_id` are both nullable (not every work order is scoped to one specific site/asset) but, when set, are cross-checked against `client_id` (and against each other) by `validate_work_order_relations`: `site_id` must belong to `client_id`; `asset_id` must belong to `client_id` and, if `site_id` is also set, to that same site. `assigned_to` is a nullable FK into `users`, validated by the same trigger to be a member of the work order's own organization (mirrors the cross-org-reference validation style `validate_contact_role_item` uses for reference-list FKs, applied here to a `memberships` check instead). `status_id` (not null, defaults to the org's default `work_order_status` item, folded into `derive_work_order_organization_id` the same way `assets.status_id`'s default is folded into `derive_asset_org_and_client`) and `priority_id` (nullable) are reference-list FKs validated by `validate_work_order_reference_items` (same structural style as `validate_asset_reference_items`). **`contract_id`** (nullable FK into `contracts`) was added by `supabase/migrations/20260823150000_contracts_core.sql` once Contracts landed (issue #33) — deferred from this migration's own design note 5 for exactly that reason; when set, it must belong to the same `client_id` as the work order, extended into the same `validate_work_order_relations` trigger (not a parallel one).
  - **RLS — the first table where the Planner/Engineer split is enforced at the database layer, not deferred to the application layer.** Implements `lib/rbac/permissions.ts`'s `planning` row exactly: **owner**/**planner** CRUD on all rows; **engineer** SELECT/UPDATE limited to rows where `assigned_to = auth.uid()` (no INSERT, no DELETE); **finance**/**administratie** SELECT-only, all rows (not assignment-scoped). This closes the "known, deliberate v1 gap" flagged in `20260822190000_clients_sites_assets.sql` design note 2 — for `work_orders`, not yet for `assets`.
  - **New helper**: `public.current_member_role(org_id uuid) returns membership_role` (`SECURITY DEFINER`, `stable`) — the caller's role in that organization, or `NULL` if they have no membership there. Documented alongside `is_member_of_org`/`is_org_owner` (see "Multi-tenancy & data isolation" above) as the reusable primitive for any RLS policy that needs to branch on *which* role the caller has, not just whether they're a member/owner — reused (not reinvented) by `contracts`' Finance-CRUD boundary below; Assets' own still-deferred Planner/Engineer split remains the next candidate.
  - **Reference lists**: `work_order_status` (New [default] → Scheduled → En Route → In Progress → Completed → Invoiced) and `work_order_priority` (Low, Normal [default], High, Urgent) — both flat (no `parent_list_key`), seeded per organization by extending `seed_default_reference_lists` per its documented extension pattern, with a one-time backfill call for existing organizations in this migration.
- `contracts` / `contract_assets` — **implemented** (issue #33, Phase 2) in `supabase/migrations/20260823150000_contracts_core.sql`, tested in `supabase/tests/database/contracts_rls.test.sql`. The core business-agreement entity with a client (service contract/maintenance agreement) — the last of the original 6 modules to land, and the SECOND table (after `work_orders`) whose RBAC matrix row is enforced as real RLS via `current_member_role`, with a NEW shape (two roles get full CRUD, not one):
  - `contracts (id, organization_id, client_id, name, type_id, sla_tier_id, billing_terms_id, start_date, end_date, auto_renew, value, notes, created_by, created_at, updated_at)`. `organization_id` is denormalized from `clients.organization_id` via `client_id` (`derive_contract_organization_id`, same shape as `sites`/`contacts`/`work_orders`); the same trigger fills in the organization's default `contract_type` item when `type_id` is omitted on insert (same fold-in-the-derive-trigger pattern as `work_orders.status_id`). `name` is a human-readable contract name/number (not auto-generated). `end_date` is nullable (open-ended contracts are real) but, when set, must be `>= start_date` (`contracts_end_date_after_start_date` check constraint). `value numeric(12,2)` is nullable.
  - `type_id` (not null), `sla_tier_id` (nullable), `billing_terms_id` (nullable) are all reference-list FKs, validated by `validate_contract_reference_items` (same structural style as `validate_asset_reference_items`/`validate_work_order_reference_items`). **`sla_tier_id` is the SECOND pilot of the dependent-reference-list mechanism** (see "Dependent reference lists" above) — `sla_tier` has `parent_list_key = 'contract_type'`, and `validate_contract_reference_items` additionally checks that the chosen `sla_tier_id` item's `parent_item_id` equals the contract's own `type_id` (mirrors `assets.subtype_id`/`type_id`'s cross-field check exactly). `billing_terms_id` is a third, independent, standalone (non-dependent) reference list.
  - **RLS — implements `lib/rbac/permissions.ts`'s `contracts` row exactly**: **owner** and **finance** both get full CRUD on all rows (a new two-full-CRUD-roles shape, unlike `work_orders`' owner/planner pairing); **planner**/**engineer**/**administratie** are SELECT-only, all rows. Reuses `current_member_role(organization_id) in ('owner', 'finance')` for the write policies — no new helper needed, exactly the reusability `current_member_role` was built for.
  - `contract_assets (contract_id, asset_id, organization_id, created_by, created_at)` — **the first genuine many-to-many join table in this schema** (everything else so far is one-to-many via a denormalized `organization_id`). Primary key is the natural key `(contract_id, asset_id)`, no surrogate `id`. `organization_id` is denormalized from the **contract's** `organization_id` (via `contract_id`); the linked asset must additionally resolve to the same organization AND the same `client_id` as the contract (`validate_contract_asset_relations`) — you cannot link an asset from a different client onto a contract, even within the same organization. Same owner-or-finance write boundary as `contracts` itself ("if you can manage the contract, you can manage its asset links"); no UPDATE support at all (delete + re-insert to change either side of a link).
  - **Reference lists**: `contract_type` (Maintenance [default], Service, Installation, Warranty, flat), `sla_tier` (dependent on `contract_type` — Maintenance: Standard/Priority/Premium; Service: Standard/Express; Installation: Standard/Expedited; Warranty: Standard/Extended; no item marked `is_default` since the single-default-per-*list* mechanism doesn't map cleanly onto "one default tier per contract type"), and `billing_terms` (Monthly [default], Quarterly, Annually, Per-visit, One-time, flat, standalone) — all seeded per organization by extending `seed_default_reference_lists`, with a one-time backfill call for existing organizations in this migration.
- `time_entries` — **implemented** (issue #15, Phase 2) in `supabase/migrations/20260823180000_time_entries_core.sql`, tested in `supabase/tests/database/time_entries_rls.test.sql`. An engineer's labor/travel/break time logged against a work order, clocked in/out — a sub-resource of Work Orders, reusing the `planning` RBAC module rather than a new one (same reasoning `contacts` used for `clients`). The THIRD table (after `work_orders`, `contracts`) whose RBAC matrix row is real RLS via `current_member_role`, with a new shape again — engineer gets INSERT here (scoped to their own rows), unlike `work_orders`:
  - `time_entries (id, organization_id, work_order_id, user_id, entry_type_id, started_at, ended_at, notes, created_by, created_at, updated_at)`. `organization_id` is denormalized from `work_orders.organization_id` via `work_order_id` (`derive_time_entry_organization_id`, same shape as `sites`/`contacts`/`work_orders`/`contracts`); the same trigger fills in the organization's default `time_entry_type` item when `entry_type_id` is omitted on insert. `user_id` (not null) is the engineer who logged the time, validated by `validate_time_entry_relations` to be a member of the time entry's own organization (mirrors `work_orders.assigned_to`'s validation) — it's also the column engineer RLS scoping keys on. `started_at` (not null, default `now()`) / `ended_at` (nullable — null means "currently running", the clock-in/clock-out UX); when `ended_at` is set it must be `>= started_at` (`time_entries_ended_at_after_started_at` check constraint). `entry_type_id` is a reference-list FK, validated by `validate_time_entry_reference_items` (same structural style as `validate_work_order_reference_items`).
  - **RLS — implements `lib/rbac/permissions.ts`'s updated `planning` row for this sub-resource**: **owner**/**planner** CRUD on all rows; **engineer** SELECT/INSERT/UPDATE limited to rows where `user_id = auth.uid()` (their own clock-in/out; `WITH CHECK` on UPDATE re-verifies `user_id = auth.uid()` on the new row too, so an engineer cannot reassign a time entry to someone else), no DELETE; **finance**/**administratie** SELECT-only, all rows. The engineer-gets-INSERT shape is new (`work_orders`' engineer row has no INSERT at all) — it's what backs the matrix's new `create_own` action on `planning` (see RBAC matrix section below); `work_orders` itself is unaffected, still owner/planner INSERT only.
  - **Reference list**: `time_entry_type` (Labor [default], Travel, Break) — flat, seeded per organization by extending `seed_default_reference_lists`, with a one-time backfill call for existing organizations in this migration.
- `checklist_templates` / `checklist_template_items` / `work_order_checklists` / `work_order_checklist_items` — **implemented** (issue #14, Phase 2, "Checklists / inspection forms") in `supabase/migrations/20260823210000_checklists_core.sql`, tested in `supabase/tests/database/checklists_rls.test.sql`. Configurable form templates attachable to a work order, filled in by engineers with checkbox/notes completion — **photo attachments and e-signature capture are explicitly OUT of scope for this pass** (a documented follow-up needing Supabase Storage integration — buckets, signed upload URLs, storage RLS — not an oversight; likely lands as a `work_order_checklist_item_photos` child table plus a `signed_off_by`/`signed_off_at`/`signature_url` trio on `work_order_checklists`). Note: `work_orders` has no "type" field, so a checklist's applicability is driven entirely by which template a planner/owner/engineer chooses to instantiate on a given work order, not by any work-order-type matching.
  - **Two reused patterns, no new one invented**: `checklist_templates`/`checklist_template_items` are tenant-configured CONTAINER data with the exact same RLS/grant shape as `reference_lists`/`reference_list_items` (owner configures, any member reads) — not modeled as a reference list itself, since template items carry columns (`is_required`, ordering-as-a-first-class `sort_order`) a picklist item doesn't need. `work_order_checklists`/`work_order_checklist_items` are a Work Order SUB-RESOURCE in the same spirit as `time_entries`, denormalizing `organization_id` from the parent work order — but RLS mirrors `work_orders`' OWN per-role shape exactly (engineer gets no create/delete on either instance table), not `time_entries`' `create_own` carve-out.
  - `checklist_templates (id, organization_id, name, created_by, created_at, updated_at)`. RLS: SELECT any member; INSERT/UPDATE/DELETE owner only (identical to `reference_lists`).
  - `checklist_template_items (id, checklist_template_id, organization_id, label, is_required, sort_order, created_by, created_at, updated_at)` — `organization_id` denormalized from `checklist_templates` via `checklist_template_id` (`derive_checklist_template_item_org`, mirrors `derive_reference_list_item_org`). Same RLS boundary as `checklist_templates`.
  - `work_order_checklists (id, work_order_id, checklist_template_id, organization_id, assigned_to, created_by, created_at)` — at most one per work order (`unique (work_order_id)`). `organization_id` AND `assigned_to` are both denormalized from the parent work order (`derive_work_order_checklist_fields`). **`assigned_to` sync decision**: kept in sync ACTIVELY, not left to drift — `work_orders_sync_checklist_assigned_to` (an `after update of assigned_to on work_orders` trigger) re-writes `work_order_checklists.assigned_to` (and cascades into `work_order_checklist_items.assigned_to`) whenever a work order's assignment changes. Chosen over accepting drift because this denormalized column directly backs an access-control boundary (a stale value would wrongly deny the newly-assigned engineer or wrongly retain the previous one's access), unlike a purely cosmetic denormalized field — the cost (two extra single-row UPDATEs on the uncommon event of reassigning a work order that already has a checklist) is negligible. `checklist_template_id` is nullable (`on delete set null` — the template could later be deleted without destroying the historical instance) and, together with `work_order_id`, is excluded from the UPDATE column grant entirely: this table is close to fully immutable after creation (no legitimate "move this checklist" or "re-template after items are already snapshotted" action; correct a mistake via DELETE + re-INSERT, the same precedent `contract_assets` already established). Net effect: the UPDATE RLS policy exists (matching `work_orders`' shape exactly, ready for a future column) but is currently unreachable for every role alike, by design.
  - **Template-to-instance copy mechanism**: inserting a `work_order_checklists` row with a non-null `checklist_template_id` fires an `after insert ... when (new.checklist_template_id is not null)` trigger (`work_order_checklists_instantiate_items`) that calls `copy_checklist_template_items_to_work_order_checklist(work_order_checklist_id)` (`SECURITY DEFINER`), snapshotting every current `checklist_template_items` row for that template into `work_order_checklist_items` by value (not a live join — later template edits don't retroactively rewrite an in-progress instance). Chosen as a trigger, not a second RPC the API layer must remember to call, so a single `insert into work_order_checklists` always yields a fully-populated checklist.
  - `work_order_checklist_items (id, work_order_checklist_id, template_item_id, organization_id, assigned_to, label, is_required, sort_order, is_checked, checked_by, checked_at, notes, created_by, created_at, updated_at)` — the actual per-work-order checklist state. `label`/`is_required`/`sort_order` are copied BY VALUE at creation and remain independently, client-editable per instance thereafter (an owner/planner can correct an item's text or add a bespoke ad-hoc item, with `template_item_id = null`, beyond what the template had). `template_item_id` (nullable, `on delete set null`) is a historical breadcrumb only, excluded from both INSERT and UPDATE grants — populated only by the SECURITY DEFINER copy function. `checked_by`/`checked_at` are trigger-stamped (`set_checklist_item_checked_fields`, mirrors `set_created_by`'s never-client-suppliable pattern): setting `is_checked = true` stamps `checked_by = auth.uid()`/`checked_at = now()`; setting it back to `false` clears both.
  - **RLS — `work_order_checklists`/`work_order_checklist_items` both implement the same shape, mirroring `work_orders`' own per-role split exactly** (not `time_entries`'): **owner**/**planner** CRUD on all rows; **engineer** SELECT/UPDATE only on rows where `assigned_to = auth.uid()` (no INSERT, no DELETE — creating/deleting a checklist instance or its items is a planner/owner action, matching Work Orders' own create boundary); **finance**/**administratie** SELECT-only, all rows. The real, functional "engineer can update" surface is entirely on `work_order_checklist_items` (checking boxes, editing per-instance notes/label) — see the `work_order_checklists` immutability note above for why that table's own UPDATE is policy-present but grant-unreachable today.
- `quotes` / `quote_line_items` — **implemented** (issue #16, Phase 3 — "Quotes / Estimates") in `supabase/migrations/20260824090000_quotes_core.sql`, tested in `supabase/tests/database/quotes_rls.test.sql`. The pre-sale proposal builder — a quote is converted into a Work Order/Contract on customer approval. The FIFTH table pair whose RBAC matrix row is real RLS via `current_member_role`, reusing `work_orders`' exact shape (owner/planner CRUD, everyone else read-only, no per-row assignment scoping) rather than `contracts`' owner+finance shape, since a quote isn't yet revenue:
  - `quotes (id, organization_id, client_id, site_id, name, status_id, valid_until, notes, created_by, created_at, updated_at)`. `organization_id` is denormalized from `clients.organization_id` via `client_id` (`derive_quote_organization_id`, same shape as `sites`/`contacts`/`work_orders`/`contracts`); the same trigger fills in the organization's default `quote_status` item when `status_id` is omitted on insert. `name` is a human-readable quote name/number (not auto-generated). `site_id` is nullable (a quote may or may not be tied to one specific location) but, when set, must belong to `client_id` (`validate_quote_relations`, same cross-field style as `work_orders.site_id`). `status_id` (not null) is a reference-list FK validated by `validate_quote_reference_items` (same structural style as `validate_work_order_reference_items`).
  - **No stored total, by design.** `quotes` has no `total`/`amount` column at all — a quote's total is `sum(quantity * unit_price)` over its `quote_line_items`, computed on read at the application layer (a single aggregate query/view), not synced via trigger. This avoids a redundant stored value that would need a maintenance trigger on every line-item insert/update/delete for a value that's cheap to derive. **Do not "fix" this later by adding a stored total column** — that would reintroduce the exact denormalization-drift risk this design avoided; a cached total is only worth adding as a separately-reviewed decision if a real performance need (e.g. sorting a huge quotes list) ever justifies it.
  - `quote_line_items (id, quote_id, organization_id, asset_id, description, quantity, unit_price, sort_order, created_by, created_at, updated_at)` — the pricing rules within a quote. `organization_id` is denormalized from the QUOTE's `organization_id` via `quote_id` (`derive_quote_line_item_organization_id`). `asset_id` is nullable (optional context link, e.g. "this line item is for servicing this specific asset") but, when set, must belong to the QUOTE's own `client_id` (`validate_quote_line_item_relations`, mirroring `contract_assets`' asset/contract client-match check). `quantity numeric(10,2)` defaults to 1, `unit_price numeric(12,2)` defaults to 0 — neither is combined into a stored total (see above). `quote_id` is immutable after creation (excluded from the UPDATE column grant, delete + re-insert to move a line item to a different quote) — same stance as `reference_list_items.reference_list_id` / `checklist_template_items.checklist_template_id`.
  - **RLS — implements the new `quotes` module exactly like `work_orders`' `planning` row**: **owner**/**planner** CRUD on all rows (`quotes` and `quote_line_items` alike — "if you can manage the quote, you can manage its line items"); **engineer**/**finance**/**administratie** SELECT-only, all rows (no per-row assignment scoping — a quote isn't assigned to one engineer, it's a sales document any team member can see).
  - **Reference list**: `quote_status` (Draft [default] → Sent → Accepted / Rejected / Expired) — flat, seeded per organization by extending `seed_default_reference_lists`, with a one-time backfill call for existing organizations in this migration.
  - **Traceability from conversion**: `work_orders.source_quote_id` and `contracts.source_quote_id` (both nullable FKs into `quotes`, added as plain additive columns on those already-existing, already-locked-down tables) trace a Work Order/Contract created by converting an accepted quote back to that quote. When set, each must belong to the same `client_id` as the work order/contract — `validate_work_order_relations` is extended (`CREATE OR REPLACE`) with this check, and `contracts` gains a brand-new `validate_contract_relations` trigger for it (contracts previously had no relations-style cross-field trigger, only `validate_contract_reference_items` for its reference-list FKs). The actual "convert this quote into a Work Order/Contract" business logic (copying line items, setting status, etc.) is application-layer, not built by this migration.
  - **Flagged for `api-backend-engineer`**: `lib/rbac/permissions.ts` needs a new `"quotes"` entry (`Module` type + `TENANT_PERMISSIONS` matrix row: owner CRUD, planner CRUD, engineer/finance/administratie read-only — see the RBAC matrix table and note below) and `lib/rbac/features.ts` needs `"quotes"` added to both the `FeatureKey` union and `SHIPPED_FEATURES` — this exact omission (shipping a module's RLS without wiring the application-layer permission/feature-flag keys) has broken multiple modules already this session; don't repeat it a fourth time.
- `reports` (work_order_id, pdf_url, generated_at)
- `invoices` (organization_id, client_id, amount, status) — the tenant's own invoicing to its clients
- `invites (id, organization_id, email, role, invited_by, token, expires_at, accepted_at, created_at)` — **implemented** in `supabase/migrations/20260822180000_invites.sql`, issue #3/#4. Lets an existing `owner` invite an email address that has no `auth.users` row yet: `token` is an unguessable capability looked up (pre-auth) via the `get_invite_by_token(token)` SECURITY DEFINER function, and redeemed into a real `memberships` row post-auth via the `redeem_invite(token)` SECURITY DEFINER function, which enforces that the redeeming account's own email matches the invite's email. `role` reuses `membership_role` (no `platform_admin` value), so Platform Admin access is structurally unreachable through this flow.
- `organization_features` (organization_id, feature_key, enabled) — entitlements
- `subscriptions` (organization_id, stripe_customer_id, stripe_subscription_id, plan)
- `audit_log` (organization_id, actor_id, action, entity, at)

## RBAC matrix — starting point

| Module | Owner | Planner | Engineer | Finance | Administratie | Platform Admin |
|---|---|---|---|---|---|---|
| Clients | CRUD | Read | Read (assigned) | Read | Read | Read (support only) |
| Assets | CRUD | Read/Update | Read/Update (assigned) | Read | Read | Read |
| Contracts | CRUD | Read | Read | CRUD | Read | Read |
| Planning | CRUD | CRUD | Read/Update/Create own | Read | Read | — |
| Checklists | CRUD | CRUD | Read/Update own | Read | Read | — |
| Quotes | CRUD | CRUD | Read | Read | Read | — |
| Reporting | Read | Read | Create (own work orders) | Read | Read | Cross-tenant |
| Dashboarding | Configure | View | View (own) | View | View | Cross-tenant |
| Billing/Facturatie | Read | — | — | CRUD | CRUD | Platform billing only |
| Platform | — | — | — | — | — | Read |

Encode this as a single config object (`lib/rbac/permissions.ts`), not scattered `if (role === ...)` checks. Enforce it both server-side and in RLS.

**Implemented** (issue #4): `lib/rbac/permissions.ts` encodes the table above verbatim as `TENANT_PERMISSIONS` (+ a `PLATFORM_ADMIN_PERMISSIONS` column), exposing `can(actor, module, action)`, `canAny(...)`, `allowedActions(...)`, and `canAccessModule(...)`. `actor: { role: TenantRole | null; isPlatformAdmin?: boolean }` keeps Platform Admin as an orthogonal flag, never a `TenantRole` member, per the rule above. `_own`-suffixed actions (`create_own`/`read_own`/`update_own`) mark matrix cells like "Read (assigned)"/"Read/Update/Create own" — callers must still apply the actual resource-ownership filter themselves; `can()` only tells you the verb is allowed at all.

**Planning row — enforced at the RLS layer, not just here** (issue #13): `work_orders` (see "Core schema (v1)" above) is the first module where this matrix's row is implemented as real Postgres RLS policies (via the new `current_member_role` helper), not only as this application-level `can()` check. Every other row above (Clients/Assets/Contracts/Reporting/Dashboarding) is still `can()`-only pending its own table/RLS work.

**Contracts row — the SECOND module enforced at the RLS layer** (issue #33): `contracts`/`contract_assets` (see "Core schema (v1)" above) reuse `current_member_role` for a new shape — **owner** and **finance** both get full CRUD (`current_member_role(organization_id) in ('owner', 'finance')`), **planner**/**engineer**/**administratie** are read-only. Clients/Assets/Reporting/Dashboarding remain `can()`-only pending their own table/RLS work.

**Planning row updated — engineer gains `create_own`** (issue #15, Time Tracking on Work Orders): `time_entries` (see "Core schema (v1)" above), the THIRD table with real RLS via `current_member_role`, is what backs this. The engineer cell for `planning` is now `Read/Update/Create own` (was `Read/Update own`) — an engineer can create a `time_entries` row for themselves (clock in/out on a work order they're doing), but this does NOT extend to `work_orders` itself: creating a Work Order is still gated on plain `create`, which engineer still lacks, so `work_orders`' own INSERT policy is unchanged (owner/planner only).

**Checklists — the FOURTH table pair with real RLS via `current_member_role`** (issue #14): `work_order_checklists`/`work_order_checklist_items` (see "Core schema (v1)" above) reuse `work_orders`' OWN per-role shape exactly (owner/planner CRUD, engineer read/update-assigned-only with NO create/delete, finance/administratie read-only) — deliberately NOT `time_entries`' `create_own` shape, since creating a checklist instance is a planner/owner action here. **Resolved** (issue #14, second stage — `api-backend-engineer`): the flagged mismatch is settled by introducing a **distinct `checklists` module** in `lib/rbac/permissions.ts` (own row: owner/planner CRUD, engineer `read_own`/`update_own` only, finance/administratie read-only), rather than reusing `planning` — `planning`'s engineer row was widened to `create_own` for Time Tracking (see row above), and reusing it here would have incorrectly suggested an engineer can create a checklist instance too, which the RLS above does not allow. `checklists` is also registered as its own `FeatureKey`/`SHIPPED_FEATURES` entry in `lib/rbac/features.ts`. Server Actions: `lib/checklist-templates/actions.ts` (template CRUD, gated on the `settings` module — same tier as `reference_lists`, per that module's owner-configures/any-member-reads shape) and `app/(app)/work-orders/checklist-actions.ts` (the work-order-instance CRUD, gated on the new `checklists` module).

**Quotes — a NEW module, the FIFTH table pair with real RLS via `current_member_role`** (issue #16, Phase 3): `quotes`/`quote_line_items` (see "Core schema (v1)" above) reuse `work_orders`' exact `planning`-row shape — owner/planner CRUD, engineer/finance/administratie read-only, no per-row assignment scoping — NOT `contracts`' owner+finance shape, since a quote is a pre-sale proposal, not yet revenue. **Resolved** (issue #16, second stage — `api-backend-engineer`): a distinct `quotes` module entry has been added to `lib/rbac/permissions.ts`'s `Module` type + `TENANT_PERMISSIONS` matrix (owner CRUD, planner CRUD, engineer/finance/administratie read-only, matching the RLS shape above verbatim) and `"quotes"` has been added to `features.ts`'s `FeatureKey` union + `SHIPPED_FEATURES` set — confirmed present in both files by `qa-reviewer`.

**Platform — a NEW module, `can()`-only (no RLS table of its own)** (issue #45, Platform Admin managing tenants via the Clients module): backs `app/(app)/platform-settings/page.tsx`, a stub landing page for Platform Admin's own cross-tenant settings, gated `if (!session.isPlatformAdmin) notFound()` directly (not the usual `hasFeature(organization, ...)` two-step every other module page uses, since `session.organization` is `null` for a platform-admin-only account and `hasFeature` always returns `false` when `organization` is `null`) followed by `canAccessModule(actor, "platform")` for the actual authorization check, same as every other module page. Every `TenantRole` gets `NONE` (Platform is never a tenant-role concern, same reasoning as the `TenantRole` union excluding `is_platform_admin` entirely); `PLATFORM_ADMIN_PERMISSIONS.platform = READ_ONLY`. The `components/shell/nav-items.ts` "Platform" nav group/item is likewise not gated through the normal per-item `hasFeature()` resolution — `resolveNavItems()` special-cases `moduleKey === "platform"` to resolve `enabled` from `isPlatformAdmin` (threaded through as a new second parameter) instead, for the same `organization: null` reason.

## Feature flags
- `organization_features` drives UI + API gating
- Stripe webhook (`customer.subscription.updated`) syncs entitlements → `organization_features`
- Platform Admin UI can override per tenant (trials, custom deals) — logged in `audit_log`
- Every module route/component checks entitlement via one helper — `hasFeature(organization, featureKey)` — never hardcode module availability

**Implemented (Phase 0 stub, issue #4):** `lib/rbac/features.ts` exports `hasFeature(organization, featureKey)` with the real call signature (`organization: { id }`, async) already wired everywhere a module gate is needed (`components/shell/nav-items.ts` → `resolveNavItems`, threaded down to the sidebar and command palette). Since `organization_features` doesn't exist until Phase 3, the current implementation ignores `organization` and returns `true` only for feature keys with a shipped implementation (today: `dashboard`). Swapping in the real `organization_features` query (documented inline as a TODO in that file) is the only change needed once Phase 3 lands — no call site should need to change.

## Premium UX requirements
- Collapsible sidebar (persisted per user), command palette, optimistic mutations
- View switcher per module: list / kanban / calendar / map (Assets, Planning)
- Skeleton loading, not spinners; route-level streaming (Suspense)
- Design tokens and components come exclusively from `@yourorg/ui` — no ad-hoc styling in the app repo

### Relational detail pages — the standard, not a special case
A flat list + a create/edit modal is never the whole answer once a record has real relationships (a client has sites and assets; a contract will have line items and linked assets; a work order will have a client, a site, an asset, and a planner). Whenever you build or touch a detail page for an entity that has related child or parent records, apply this pattern:

- **Surface the relationship in place.** A parent detail page shows its related records as tabs (`Tabs` from `@yourorg/ui`), not just a link out to another module's list. Reference implementation: `app/(app)/clients/[id]/client-detail.tsx` — a client's own fields, plus `Sites` and `Assets` tabs on the same page, with `SitesPanel`'s per-site asset count jumping straight into the matching `Disclosure` group in `AssetsPanel`. That three-level hierarchy (client → sites → assets) staying visible and navigable from either tab, instead of being flattened into one generic table, is the bar — not the exception.
- **Create in context.** A record creatable from a parent's tab (e.g. adding an asset from a client's Assets tab) should open pre-scoped to that parent (client/site already set), not dump the user into a bare, disconnected form.
- **Nested groupings use `Disclosure`**, not a flat table, once a list has a natural sub-grouping (assets grouped by site, line items grouped by category, etc).
- **Breadcrumb the hierarchy**, don't just "back link" it. `BackLink` (single hop, "back to X") is right for a page with one obvious parent list. Once a page sits two or more levels deep in a real hierarchy (Clients → Acme Corp → Site X), it needs a breadcrumb trail showing the full path, not just a single hop backward — add a `Breadcrumbs` primitive to `@yourorg/ui` if one doesn't exist yet rather than approximating it with more `BackLink`s.
- **Known current gap**: `app/(app)/assets/[id]/page.tsx` (asset detail) does not meet this bar yet — it shows `Client`/`Site` as plain text/link `DetailRow`s with only a single `BackLink`, instead of the richer treatment `client-detail.tsx` gets. Bring it in line with the pattern above rather than treating it as acceptable prior art.

Before building a new module (Contracts, Planning, Reporting — see `docs/ROADMAP.md`), identify its real relationships first and design the detail page's tabs/breadcrumbs/nesting up front — don't ship the flat version now and "add relations later."

#### `DetailHero` — the standard header for a top-level entity's detail page
The "Option C" editorial header (`DetailHero`, `packages/ui/src/components/detail-hero.tsx`) is the canonical header pattern for a top-level entity's detail page — an initials hero mark (`Avatar` `size="lg"`, a 56px rounded-square with a serif-face initial, deliberately breaking from `Avatar`'s sm/md sans circle for the statement/hero context), the entity's name as `Heading level={1}`, a dot-separated `meta` line (email/phone/primary address for a client; the equivalent 2-3 identifying facts for another entity), `badges` appended after with no leading dot, and a right-aligned `actions` slot (Edit/Delete). It replaces the earlier compact-`Toolbar`-info-row treatment.

- **Generic by design**: `DetailHero`'s props (`avatarLabel`, `title`, `meta`, `badges`, `actions`) carry no Client-specific naming — any top-level entity's detail page (Assets, Work Orders, Contracts, Quotes, once those get their own detail pages) reuses it as-is.
- **Reference implementations**: `packages/ui/stories/EditorialDetailPage.stories.tsx` is the generic template (`DetailHero` + `Tabs`, no map) to copy for a new module's detail page. `app/(app)/clients/[id]/client-detail.tsx` is the real, shipped implementation for Clients.
- **Client-specific addition, not part of the generic pattern**: the Sites tab's `.ui-sites-grid` layout (table + a narrow "Locations" map side-card, `app/(app)/clients/[id]/sites-panel.tsx`) is specific to Clients (and any future address-bearing entity) — it is not something Assets/Work Orders/Contracts/Quotes detail pages should expect to inherit just because they use `DetailHero`.

### Popup vs. full page — pick by weight, not habit
A `Dialog` is for a **small, secondary list's** create/edit form — a sub-entity reached from a tab on some other record's detail page (Contacts on a client, Sites on a client, an item in a Settings reference list). It is **not** the right pattern for a top-level module's own primary record.

- **Top-level module entities** — Clients, Assets, Contracts, Planning/Work Orders, and future Sales/Ops modules like Quotes or Projects/Orders (see `docs/ROADMAP.md`) — get a **real page** for create/edit (`/assets/new`, `/assets/[id]/edit`, etc.), not a modal. These records carry enough fields, relationships, and (once Work Orders/Contracts land) enough sub-content of their own that a popup is the wrong container — a page gives room to breathe, a real URL to link/bookmark, and space for the record's own tabs/sections without nesting a `Tabs`-inside-a-`Dialog`.
- **Explicit, confirmed overrides — Clients only, so far**: Clients is the one top-level entity where the product owner has explicitly asked for the slide-in-panel treatment instead of a real page, for both create and edit. Issue #43 ("Nieuwe klant toevoegen toont nieuwe popup") moved client CREATE to `NewClientPanel` (`app/(app)/clients/new-client-panel.tsx`, a `Dialog` `size="panel"`), deleting the old `/clients/new` route. Issue #46 ("Client edit pagina moet ook een slide in popup worden") did the same for client EDIT — `EditClientPanel` (`app/(app)/clients/edit-client-panel.tsx`) replaced the old full-page `/clients/[id]/edit` route (also deleted; `ClientForm`/`client-form.tsx`, that page's form component, was removed too since the panel was its only remaining caller). Both panels are opened from local component state (`client-detail.tsx`'s own `useState` on the client's own detail page; a lifted `editTarget`/`onEdit` state pair in `clients-explorer.tsx`, mirroring the existing `deleteTarget`/`onDelete` pattern, for the clients list/kanban). Treat this as a Clients-specific carve-out, not a precedent to silently extend to Assets/Contracts/Work Orders/Quotes without the same explicit sign-off.
- **Sub-entities reached from a tab** (Contacts, Sites) stay dialogs — they're small, single-purpose, and scoped to a parent that's already on screen; a full-page round trip would be overkill for them.
- **Known current gap**: the asset create/edit dialog (`app/(app)/assets/components/asset-form-dialog.tsx`) is a top-level-entity form still built as a `Dialog`. Convert it to a real page; leave `site-form-dialog.tsx` and `contact-form-dialog.tsx` as dialogs (they're the small-sub-entity case this rule carves out).

## Domain completeness

`docs/BUSINESS-PLAN.md` §4 was widened 2026-08-23 after review: the original module list only had each module's MVP slice (e.g. a client with one email/phone, no contact persons) instead of the realistic domain a premium comparable product has. This is now a standing requirement, not a one-time fix:

- **Before modeling a new module or entity**, briefly research 1-2 comparable premium SaaS products in that domain (ServiceTitan/Jobber/Housecall Pro/Salesforce Field Service-tier, or the closest equivalent for a non-FSM concern) and default to that realistic breadth of sub-entities/fields, not the minimum needed to satisfy the current user story. `docs/ROADMAP.md` names the modules already identified this way (Work Orders, Contacts, Quotes, Invoicing, Preventive Maintenance, Inventory, Customer Portal, etc.) — check there first before assuming something is out of scope.
- **Every categorical/type/status field is tenant-configurable** via `reference_lists`/`reference_list_items` (`supabase/migrations/20260822200000_reference_lists.sql`) — never a hardcoded enum or free text.
- **Check whether a reference list depends on another one** before modeling it as a flat list — e.g. Asset Sub-type's valid values depend on which Asset Type is selected. Use the dependent-list mechanism (`reference_lists.parent_list_key` + `reference_list_items.parent_item_id`, validated the same way `validate_asset_reference_items` validates cross-list correctness) instead of one flat list mixing every possible value, or instead of inventing a one-off parent/child table pair.
- **New sub-entities follow the "Relational detail pages" standard above**: surfaced as a `Tabs` entry on their parent, created in-context, not bolted on as a disconnected flat list reachable only via its own top-level route.

## Design system consumption
`@yourorg/ui` lives in this repo at `packages/ui`, as a real npm workspace package (root `package.json` `"workspaces": ["packages/*"]`), not a separate repo or private registry — there is no separate design-system repo, and none is planned for now. This is a deliberate choice, not a temporary stand-in (that was the old `vendor/yourorg-ui-stub`, now deleted): keeping it in-repo means the app and its design system iterate in the same PR.

- **Package structure**: TypeScript/TSX source in `packages/ui/src`, built with `tsup` to `packages/ui/dist` (both compiled JS — ESM + a CJS fallback for non-Next tooling — and generated `.d.ts` declarations straight from the `.tsx` source, so component implementations themselves are type-checked, not just call sites). `dist/` is gitignored, never committed — it's a build artifact, same as `.next/`.
- **The app still only ever imports from `@yourorg/ui`** / `@yourorg/ui/icons` / `@yourorg/ui/styles.css` (CLAUDE.md rule 4 is satisfied by this package boundary) — never reach into `packages/ui/src` or `dist` directly.
- **Build wiring**: `npm run build`/`npm run typecheck` at the root each have a `pre*` script (`prebuild`/`pretypecheck`) that builds `packages/ui` first, since the app consumes it like a normal dependency (needs `dist/` to exist), not via source transpilation. `npm run dev` has an equivalent `predev` step. CI (`.github/workflows/ci.yml`) builds `packages/ui` explicitly before lint/typecheck/build for the same reason.
- **Two "use client" boundaries**: `ThemeProvider`/`useTheme` and `Tabs` are the only stateful/interactive primitives in the package; everything else is a plain presentational function so it stays safe to render from Server Components. See the top-of-file comment in `packages/ui/tsup.config.ts` for why those two need their own dedicated build entries (a real, empirically-discovered esbuild/RSC directive-bundling gotcha, not incidental complexity).
- Fixes/changes to a component go into `packages/ui` directly, in the same PR as the app change that needed them — there's no "upstream repo" to send them to anymore.
